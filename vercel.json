{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"feat/admin-dashboard-expansion\" ] && ! printf '%s' \"$VERCEL_GIT_COMMIT_MESSAGE\" | grep -Fq '[preview]'; then exit 0; else exit 1; fi",
  "crons": [
    {
      "path": "/api/internal/melhor-envio/refresh",
      "schedule": "17 3 * * *"
    }
  ]
}
